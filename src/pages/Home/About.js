import React, { useState } from 'react';

const About = ({name}) => {
    return(
        <div>
            <h1> Hi! {name}!</h1>
            <h1> This is about page.</h1>
        </div>
    )
}

export default About;